# 教师实验室项目结构说明

## 📁 文件夹结构

```
D:\teacherlab教师实验室\
├── teacherlab-main\              # ✅ 主仓库（Git仓库，v2.0 React版本）
│   ├── .git\                     # Git版本控制
│   ├── .github\                  # GitHub Actions 工作流
│   ├── src\                      # React 源代码
│   ├── public\                   # 静态资源
│   ├── package.json              # 项目依赖
│   └── vite.config.ts            # Vite 配置
│
├── teacherlab-v1.0-backup\       # v1.0 纯HTML版本备份
│   ├── index.html                # 旧版主页
│   ├── weixinID.jpg              # 微信二维码
│   └── ...
│
├── teacherlab-rewrite\           # ⚠️ 开发副本（已废弃）
│   └── [已合并到主仓库]
│
├── pdf-to-ppt\                   # PDF转PPT工具项目
├── recitation-management\        # 背诵管理系统项目
├── wrong-questions-statistics\   # 错题统计系统项目
└── ...
```

## 🎯 Git 仓库位置

**主仓库：** `D:\teacherlab教师实验室\teacherlab-main`

- **远程地址：** `https://github.com/19827092373/teacherlab.git`
- **当前分支：** `main` (v2.0 React版本)
- **版本标签：**
  - `v1.0` - 纯HTML版本（备份）
  - `v2.0` - React版本（当前）

## 📝 开发指南

### 本地开发

```bash
# 进入主仓库
cd D:\teacherlab教师实验室\teacherlab-main

# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build
```

### Git 操作

```bash
# 查看当前状态
git status

# 查看分支
git branch -a

# 查看版本标签
git tag -l

# 切换到v1.0版本（只读）
git checkout v1.0

# 切换回v2.0版本
git checkout main
```

## 🔄 部署流程

推送代码到 `main` 分支会自动触发 GitHub Actions 部署：

1. 自动构建项目 (`npm run build`)
2. 上传到服务器 (`/www/wwwroot/teacherlab.cn`)
3. 重启 Nginx

查看部署状态：
```
https://github.com/19827092373/teacherlab/actions
```

## 📦 版本历史

- **v1.0** (2025-01-07)
  - 纯HTML单页应用
  - 静态内容展示
  - 备份位置：`teacherlab-v1.0-backup/`

- **v2.0** (2026-01-13) 🎉 当前版本
  - React + Vite + Tailwind CSS
  - 6个工具卡片展示
  - 动态统计动画
  - 响应式设计
  - SEO优化
  - 交互优化

## 🚨 回滚到 v1.0

如果需要回滚到旧版本：

```bash
cd D:\teacherlab教师实验室\teacherlab-main

# 方法1: 切换到v1.0标签（本地查看）
git checkout v1.0

# 方法2: 回滚服务器到v1.0
git checkout v1.0
git push -f origin main
```

---

**最后更新：** 2026-01-13
**当前版本：** v2.0
**维护者：** 19827092373
