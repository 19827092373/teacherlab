# TeacherLab.cn 宝塔面板部署指南

## 📋 项目结构说明

- **主站（门户网站）**：`teachlab` Git仓库
- **工具1**：PDF转PPT（React项目）
- **工具2**：错题管理（HTML静态网站）
- **工具3**：英语背诵管理统计系统（Vue3项目）

## 🎯 部署顺序（重要！）

### ✅ 正确顺序：
1. **先配置服务器和宝塔面板**（可以用IP访问测试）
2. **再配置DNS解析**（域名指向服务器IP）
3. **最后配置SSL证书**（HTTPS）

### ❌ 错误顺序：
- 先配置DNS → 服务器还没准备好 → 域名无法访问

---

## 第一步：服务器环境准备

### 1.1 选择操作系统镜像

**推荐**：**CentOS 7.9** 或 **Ubuntu 22.04 LTS**

国内云服务器镜像选择：
- 阿里云：CentOS 7.9 64位（宝塔推荐）
- 腾讯云：CentOS 7.9 64位
- 华为云：CentOS 7.9 64位

### 1.2 安装宝塔面板

**在服务器SSH中执行：**

```bash
# CentOS系统
yum install -y wget && wget -O install.sh https://download.bt.cn/install/install_6.0.sh && sh install.sh

# Ubuntu/Debian系统
wget -O install.sh https://download.bt.cn/install/install-ubuntu_6.0.sh && sudo bash install.sh
```

**安装完成后会显示：**
- 面板地址：`http://你的服务器IP:8888`
- 用户名和密码（请保存好！）

### 1.3 登录宝塔面板

1. 浏览器访问：`http://你的服务器IP:8888`
2. 输入用户名和密码登录
3. 首次登录会提示安装LNMP环境

### 1.4 安装LNMP环境

在宝塔面板中：

1. **选择软件版本**（推荐）：
   - Nginx：1.22 或最新稳定版
   - MySQL：5.7 或 8.0（如果不需要数据库可以跳过）
   - PHP：7.4 或 8.0（如果不需要PHP可以跳过）
   - phpMyAdmin：可选

2. **点击"一键安装"**，等待安装完成（约10-30分钟）

3. **安装Node.js版本管理器**：
   - 软件商店 → 搜索"PM2管理器" → 安装
   - 或者手动安装Node.js：
     ```bash
     # SSH中执行
     curl -fsSL https://rpm.nodesource.com/setup_18.x | bash -
     yum install -y nodejs
     # 或Ubuntu
     curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
     sudo apt install -y nodejs
     
     # 配置npm国内镜像
     npm config set registry https://registry.npmmirror.com
     ```

---

## 第二步：域名规划（DNS解析前准备）

### 方案A：子域名方案（推荐，更专业）

```
主站：     https://teacherlab.cn
工具1：    https://ppt.teacherlab.cn      (PDF转PPT)
工具2：    https://mistakes.teacherlab.cn (错题管理)
工具3：    https://recitation.teacherlab.cn (背诵管理)
```

### 方案B：路径方案（简单，一个域名）

```
主站：     https://teacherlab.cn
工具1：    https://teacherlab.cn/ppt
工具2：    https://teacherlab.cn/mistakes
工具3：    https://teacherlab.cn/recitation
```

**建议使用方案A（子域名）**，更清晰专业。

---

## 第三步：部署主站（teachlab仓库）

### 3.1 在宝塔面板创建网站

1. **网站** → **添加站点**
2. **域名**：`teacherlab.cn`（先填域名，DNS稍后配置）
3. **备注**：主站
4. **根目录**：`/www/wwwroot/teacherlab.cn`
5. **FTP**：不创建（使用Git）
6. **数据库**：不创建（静态网站）
7. **PHP版本**：纯静态（选择"纯静态"）

### 3.2 克隆主站代码

**方式1：使用宝塔Git功能（推荐）**

1. 网站 → 找到 `teacherlab.cn` → 点击"设置"
2. 左侧菜单 → **Git** → **克隆项目**
3. 填写：
   - **Git仓库地址**：`https://gitee.com/你的用户名/teachlab.git`（或GitHub地址）
   - **分支**：`main` 或 `master`
   - **拉取方式**：HTTPS（如果私有仓库，需要配置SSH密钥）
4. 点击"克隆"
5. 克隆完成后，设置**Web目录**为仓库根目录（通常是 `/www/wwwroot/teacherlab.cn`）

**方式2：使用SSH手动克隆**

```bash
cd /www/wwwroot/teacherlab.cn
git clone https://gitee.com/你的用户名/teachlab.git .
# 或使用SSH
git clone git@gitee.com:你的用户名/teachlab.git .
```

### 3.3 配置主站根目录

如果主站的HTML文件在子目录（如 `teacherelab` 文件夹），需要：

1. 网站 → `teacherlab.cn` → 设置 → **网站目录**
2. 修改为：`/www/wwwroot/teacherlab.cn/teacherelab`（根据实际路径调整）
3. 保存

---

## 第四步：部署工具1 - PDF转PPT

### 4.1 创建网站

1. **网站** → **添加站点**
2. **域名**：`ppt.teacherlab.cn`
3. **根目录**：`/www/wwwroot/ppt.teacherlab.cn`
4. **PHP版本**：纯静态

### 4.2 上传项目文件

**方式1：使用宝塔文件管理器**

1. 文件 → 进入 `/www/wwwroot/ppt.teacherlab.cn`
2. 上传 `习题转ppt/teacher-ppt-splitter2` 文件夹内容

**方式2：使用Git（如果项目在仓库中）**

```bash
cd /www/wwwroot/ppt.teacherlab.cn
git clone <PDF转PPT项目仓库地址> .
```

### 4.3 构建React项目

**在SSH中执行：**

```bash
cd /www/wwwroot/ppt.teacherlab.cn
npm install
npm run build
```

### 4.4 配置网站目录

1. 网站 → `ppt.teacherlab.cn` → 设置 → **网站目录**
2. 修改为：`/www/wwwroot/ppt.teacherlab.cn/dist`
3. 保存

---

## 第五步：部署工具2 - 错题管理

### 5.1 创建网站

1. **网站** → **添加站点**
2. **域名**：`mistakes.teacherlab.cn`
3. **根目录**：`/www/wwwroot/mistakes.teacherlab.cn`
4. **PHP版本**：纯静态

### 5.2 上传项目文件

1. 文件 → 进入 `/www/wwwroot/mistakes.teacherlab.cn`
2. 上传 `错题统计` 文件夹的所有内容

---

## 第六步：部署工具3 - 背诵管理统计系统

### 6.1 创建网站

1. **网站** → **添加站点**
2. **域名**：`recitation.teacherlab.cn`
3. **根目录**：`/www/wwwroot/recitation.teacherlab.cn`
4. **PHP版本**：纯静态

### 6.2 上传项目文件

1. 文件 → 进入 `/www/wwwroot/recitation.teacherlab.cn`
2. 上传 `背诵管理统计系统` 文件夹内容

### 6.3 构建Vue3项目

**在SSH中执行：**

```bash
cd /www/wwwroot/recitation.teacherlab.cn
npm install
npm run build
```

### 6.4 配置网站目录

1. 网站 → `recitation.teacherlab.cn` → 设置 → **网站目录**
2. 修改为：`/www/wwwroot/recitation.teacherlab.cn/dist`
3. 保存

---

## 第七步：配置DNS解析（现在可以配置了！）

### 7.1 在域名服务商处添加解析记录

登录你的域名服务商（如阿里云、腾讯云、GoDaddy等），添加以下DNS记录：

#### A记录（主域名）
```
类型：A
主机记录：@
记录值：你的服务器IP地址
TTL：600（或默认）
```

#### A记录（子域名）
```
类型：A
主机记录：ppt
记录值：你的服务器IP地址
TTL：600

类型：A
主机记录：mistakes
记录值：你的服务器IP地址
TTL：600

类型：A
主机记录：recitation
记录值：你的服务器IP地址
TTL：600
```

### 7.2 等待DNS生效

- 通常5-30分钟生效
- 可以使用 `nslookup ppt.teacherlab.cn` 检查是否解析成功

### 7.3 测试访问

DNS生效后，访问：
- `http://teacherlab.cn`
- `http://ppt.teacherlab.cn`
- `http://mistakes.teacherlab.cn`
- `http://recitation.teacherlab.cn`

---

## 第八步：配置SSL证书（HTTPS）

### 8.1 使用宝塔SSL功能（推荐）

1. **网站** → 选择网站 → **设置** → **SSL**
2. 选择 **Let's Encrypt**（免费证书）
3. **域名**：勾选所有域名
   - `teacherlab.cn`
   - `ppt.teacherlab.cn`
   - `mistakes.teacherlab.cn`
   - `recitation.teacherlab.cn`
4. 点击 **申请**
5. 申请成功后，**开启强制HTTPS**

### 8.2 为每个子域名单独申请证书

如果Let's Encrypt不支持通配符，需要为每个域名单独申请：

1. 主站：`teacherlab.cn` → SSL → Let's Encrypt → 申请
2. `ppt.teacherlab.cn` → SSL → Let's Encrypt → 申请
3. `mistakes.teacherlab.cn` → SSL → Let's Encrypt → 申请
4. `recitation.teacherlab.cn` → SSL → Let's Encrypt → 申请

每个都开启**强制HTTPS**。

---

## 第九步：性能优化（可选）

### 9.1 开启Gzip压缩

1. 网站 → 选择网站 → **设置** → **性能**
2. 开启 **Gzip压缩**

### 9.2 配置静态资源缓存

在网站设置 → **配置文件**中添加：

```nginx
# 静态资源缓存
location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
    expires 30d;
    add_header Cache-Control "public, immutable";
    access_log off;
}
```

### 9.3 开启CDN（可选）

如果访问量大，可以考虑：
- 阿里云CDN
- 腾讯云CDN
- 七牛云CDN

---

## 第十步：设置自动更新（Git自动拉取）

### 10.1 配置Git Webhook（推荐）

1. 网站 → `teacherlab.cn` → 设置 → **Git**
2. 开启 **Webhook**
3. 复制Webhook地址
4. 在Git仓库（Gitee/GitHub）中添加Webhook：
   - URL：粘贴复制的地址
   - 触发事件：Push事件

### 10.2 设置定时任务（备选）

1. 宝塔面板 → **计划任务**
2. 添加任务：
   - **任务类型**：Shell脚本
   - **任务名称**：更新主站代码
   - **执行周期**：每天 2:00
   - **脚本内容**：
     ```bash
     cd /www/wwwroot/teacherlab.cn
     git pull origin main
     ```

---

## 📝 部署检查清单

- [ ] 服务器已安装宝塔面板
- [ ] LNMP环境已安装
- [ ] Node.js已安装并配置国内镜像
- [ ] 主站网站已创建并部署
- [ ] PDF转PPT网站已创建并构建
- [ ] 错题管理网站已创建并部署
- [ ] 背诵管理网站已创建并构建
- [ ] DNS解析已配置（A记录）
- [ ] DNS已生效（可以ping通）
- [ ] SSL证书已申请并配置
- [ ] HTTPS强制跳转已开启
- [ ] 所有网站可以正常访问

---

## 🔧 常见问题

### Q1: DNS解析后无法访问？

**检查：**
1. DNS是否生效：`nslookup teacherlab.cn`
2. 服务器防火墙是否开放80/443端口
3. 宝塔面板 → **安全** → 检查端口是否开放
4. Nginx是否正常运行：`systemctl status nginx`

### Q2: 构建失败（npm install报错）？

**解决：**
```bash
# 清理缓存
npm cache clean --force

# 使用国内镜像
npm config set registry https://registry.npmmirror.com

# 删除node_modules重新安装
rm -rf node_modules package-lock.json
npm install
```

### Q3: 内存不足（2G内存）？

**优化：**
1. 关闭不必要的服务
2. 在本地构建后上传dist文件夹（不占用服务器内存）
3. 使用 `npm install --production` 减少依赖

### Q4: 403 Forbidden错误？

**解决：**
1. 检查文件权限：`chmod -R 755 /www/wwwroot/teacherlab.cn`
2. 检查Nginx用户权限
3. 检查网站目录路径是否正确

### Q5: Vue/React路由404？

**解决：**
在网站设置 → **配置文件**中添加：

```nginx
location / {
    try_files $uri $uri/ /index.html;
}
```

---

## 📞 需要帮助？

如果遇到问题，可以：
1. 查看宝塔面板日志：**日志** → **网站日志**
2. 查看Nginx错误日志：`/www/server/nginx/logs/error.log`
3. 检查服务器资源：**监控** → **系统监控**

---

## 🎉 部署完成！

部署成功后，你的网站结构：

```
https://teacherlab.cn              → 主站（门户）
https://ppt.teacherlab.cn          → PDF转PPT工具
https://mistakes.teacherlab.cn     → 错题管理工具
https://recitation.teacherlab.cn   → 背诵管理工具
```

祝部署顺利！🚀

